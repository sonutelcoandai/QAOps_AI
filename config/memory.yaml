memory:

  default_provider: chroma

  providers:

    chroma:
      enabled: true

    qdrant:
      enabled: false

    pinecone:
      enabled: false

    redis:
      enabled: false

    postgres:
      enabled: false